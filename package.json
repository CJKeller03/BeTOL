{
  "name": "api",
  "version": "1.0.0",
  "description": "Send a webp image from a random match to be tagged and save the returned data.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node_modules/.bin/nodemon app.js --exec babel-node --"
  },
  "author": "Caleb Keller",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "body-parser": "^1.19.0",
    "cookie-session": "^1.3.3",
    "express": "^4.17.3",
    "rimraf": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
